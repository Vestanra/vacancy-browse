import { TableFeeds } from "../components/Feeds/TableFeeds";
import { Layout } from "../components/Layout";

export const HomePage = () => {

    return (
        <Layout>
            <TableFeeds/>
        </Layout>       
    )
};
